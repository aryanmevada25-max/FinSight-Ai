import { useEffect, useState } from "react";
import { motion } from "framer-motion";
import { Download, FileSpreadsheet, FileText, TrendingUp } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { api, money } from "../api";

export default function Reports(){const [data,setData]=useState(null);useEffect(()=>{api("/dashboard").then(setData)},[]);if(!data)return <div className="shimmer mx-auto h-[34rem] max-w-6xl rounded-3xl"/>;const chart=data.charts.monthlySpending.labels.map((label,i)=>({label,expense:data.charts.monthlySpending.values[i]}));return <motion.div initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} className="mx-auto max-w-7xl"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><span className="text-xs font-semibold uppercase tracking-[.2em] text-cyan">Deep visibility</span><h2 className="mt-2 text-3xl font-bold">Analytics & reports</h2><p className="mt-2 text-sm text-muted">Turn financial activity into a readable story.</p></div><div className="flex gap-2"><a href="/reports/export.csv" className="flex items-center gap-2 rounded-xl border border-white/10 bg-white/[.04] px-4 py-3 text-xs font-semibold transition hover:border-cyan/30"><FileSpreadsheet size={16}/> CSV</a><a href="/reports/export.pdf" className="flex items-center gap-2 rounded-xl bg-primary px-4 py-3 text-xs font-semibold shadow-glow"><FileText size={16}/> PDF</a></div></header><div className="mt-7 grid gap-4 sm:grid-cols-3"><Kpi label="Net position" value={data.metrics.balance}/><Kpi label="Income tracked" value={data.metrics.income}/><Kpi label="Expenses tracked" value={data.metrics.expense}/></div><section className="glass mt-5 rounded-[28px] p-5 sm:p-7"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Monthly expense analysis</h3><p className="mt-1 text-xs text-muted">Spending movement across six months</p></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary/10 text-cyan"><TrendingUp size={19}/></span></div><div className="mt-6 h-80"><ResponsiveContainer><AreaChart data={chart} margin={{left:-14,right:10}}><defs><linearGradient id="report-area" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#22D3EE" stopOpacity=".32"/><stop offset="1" stopColor="#22D3EE" stopOpacity="0"/></linearGradient></defs><CartesianGrid stroke="rgba(148,163,184,.07)" vertical={false}/><XAxis dataKey="label" axisLine={false} tickLine={false} stroke="#64748b"/><YAxis axisLine={false} tickLine={false} stroke="#64748b"/><Tooltip contentStyle={{background:"#080d1c",border:"1px solid rgba(255,255,255,.1)",borderRadius:14}} formatter={v=>money(v)}/><Area type="monotone" dataKey="expense" stroke="#22D3EE" strokeWidth={3} fill="url(#report-area)" animationDuration={1300}/></AreaChart></ResponsiveContainer></div></section></motion.div>}
function Kpi({label,value}){return <motion.div whileHover={{y:-4}} className="glass rounded-3xl p-5"><p className="text-xs text-muted">{label}</p><p className="mt-2 text-2xl font-bold">{money(value)}</p><p className="mt-3 flex items-center gap-1 text-[11px] text-success"><TrendingUp size={13}/> Updated in real time</p></motion.div>}
